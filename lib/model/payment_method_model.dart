// Currently unused

class PaymentMethod {
  final String iconPath;
  final String bankName;
  final String bankDescription;
  bool isSelected;

  PaymentMethod(this.iconPath, this.bankName, this.bankDescription, this.isSelected);
}
