import 'package:flutter/material.dart';

class ProfileSettings {
  final IconData buttonIcon;
  final String buttonText;
  final Function onItemTap;

  ProfileSettings(this.buttonIcon, this.buttonText, this.onItemTap);
}
